# Archived image outputs

This folder holds **layout experiments and backups**, not the images used by the published shortcut.

- **Production** lock-screen JPEGs live in `../optimized_final_with_text/`.
- **Sources** live in `../original/`.

Subfolders (e.g. `*_tests`, `optimized_final_with_text_backup`) were generated while tuning text placement. Scripts that write here live in `scripts/archive/`. Run those scripts from the repository root (`thrasher-lockscreen/thrasher-lockscreen/`) so paths resolve correctly.
